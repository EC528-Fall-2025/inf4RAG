apiVersion: v1
kind: Service
metadata:
  name: rag-service
  namespace: vllm-inference
  labels:
    app: rag
spec:
  selector:
    app: rag
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  type: ClusterIP
